import {
  default as clock,
  default as events,
  default as friends,
  default as play,
} from "../public/home/icons/clock.png";

export const icons = {
  home: {
    clock: clock,
    events: events,
    friends: friends,
    play: play,
  },
};
